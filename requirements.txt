pynput>=1.8.2,<2
pyperclip>=1.9,<2
pystray>=0.19.5,<1
Pillow>=10,<12
tkinterdnd2>=0.4,<1
pywin32>=306; sys_platform == "win32"
# pynput and pystray pull most of these in already, but the app calls them
# directly - AppKit for the image clipboard, Quartz for the screen grab and the
# paste keystroke, ApplicationServices for the Accessibility check - so they are
# named here rather than left to another package's dependency list.
pyobjc-framework-Cocoa>=9; sys_platform == "darwin"
pyobjc-framework-Quartz>=9; sys_platform == "darwin"
pyobjc-framework-ApplicationServices>=9; sys_platform == "darwin"
