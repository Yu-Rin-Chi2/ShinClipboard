pynput>=1.8.2,<2
pyperclip>=1.9,<2
pystray>=0.19.5,<1
Pillow>=10,<12
tkinterdnd2>=0.4,<1
pywin32>=306; sys_platform == "win32"
# The Windows 11 look for the ttk widgets, on macOS too so both look the same.
sv-ttk>=2.6,<3
# OCR of a clipboard image with the engine built into Windows (Windows.Media.Ocr).
winrt-runtime>=3,<4; sys_platform == "win32"
winrt-Windows.Media.Ocr>=3,<4; sys_platform == "win32"
winrt-Windows.Graphics.Imaging>=3,<4; sys_platform == "win32"
winrt-Windows.Storage.Streams>=3,<4; sys_platform == "win32"
winrt-Windows.Foundation>=3,<4; sys_platform == "win32"
winrt-Windows.Foundation.Collections>=3,<4; sys_platform == "win32"
winrt-Windows.Globalization>=3,<4; sys_platform == "win32"
# pynput and pystray pull most of these in already, but the app calls them
# directly - AppKit for the image clipboard, Quartz for the screen grab and the
# paste keystroke, ApplicationServices for the Accessibility check - so they are
# named here rather than left to another package's dependency list.
pyobjc-framework-Cocoa>=9; sys_platform == "darwin"
pyobjc-framework-Quartz>=9; sys_platform == "darwin"
pyobjc-framework-ApplicationServices>=9; sys_platform == "darwin"
